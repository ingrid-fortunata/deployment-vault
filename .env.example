DATABASE_URL="postgresql://postgres:password@localhost:5432/deployment_vault?schema=public"

# Generate with: openssl rand -base64 32
JWT_SECRET=""

# Generate with: openssl rand -hex 32  (must be exactly 64 hex chars = 32 bytes)
ENCRYPTION_KEY=""
